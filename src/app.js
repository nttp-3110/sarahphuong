import React from 'react';
import ReactDOM from 'react-dom';
import Router from './router';

const page = document.getElementById('page'); // eslint-disable-line

ReactDOM.render(
    <Router />,
    page
);
